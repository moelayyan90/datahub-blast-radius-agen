const q=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let lastAnalysis=null;
q('#run').addEventListener('click',async()=>{const btn=q('#run');btn.disabled=true;btn.textContent='Tracing DataHub lineage…';q('#result').innerHTML='<div class="empty"><div class="radar"></div><p>Reading downstream context…</p></div>';try{const r=await fetch('/api/analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({urn:q('#urn').value,changeType:q('#changeType').value,detail:q('#detail').value,column:q('#column').value})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Analysis failed');lastAnalysis=d;const rc=d.risk.level==='CRITICAL'?'risk-critical':d.risk.level==='HIGH'?'risk-high':'';const modeWarning=d.liveError?`<div class="warning"><b>Live DataHub query failed.</b> Showing labelled demo metadata instead. ${esc(d.liveError)}</div>`:'';q('#result').innerHTML=`<div class="panel-title"><span>2</span> Impact decision <small class="mode">${esc(d.mode)}</small></div>${modeWarning}<div class="metrics"><div class="metric"><small>Risk score</small><strong>${d.risk.value}/100</strong></div><div class="metric"><small>Decision</small><strong class="${rc}">${esc(d.risk.decision)}</strong></div><div class="metric"><small>Downstream assets</small><strong>${d.assets.length}</strong></div></div><h3>Blast radius</h3>${d.assets.length?d.assets.map(a=>`<div class="asset"><div><b>${esc(a.name)}</b><small>${esc(a.type)} · ${esc(a.domain)} · owner: ${esc(a.owner||'UNOWNED')} · degree ${esc(a.degree)}</small></div><div>${a.tags.map(t=>`<span class="pill">${esc(t)}</span>`).join('')}</div></div>`).join(''):'<p class="hint">No downstream assets returned by the live DataHub lineage query.</p>'}<h3>Generated safe-migration plan</h3><ol class="plan">${d.actions.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><div class="artifact"><div><small>Generated artifact</small><b>Pre-merge contract test</b></div><pre>${esc(d.generatedArtifacts?.contractTest||'')}</pre></div><div class="artifact"><div><small>Generated policy</small><b>Machine-readable merge decision</b></div><pre>${esc(d.generatedArtifacts?.policyJson||'')}</pre></div><button id="persist" class="secondary">Write review back to DataHub</button><p id="persistStatus" class="hint">In demo mode this previews the write-back. The public web adapter uses GraphQL when configured; the production agent path uses the official DataHub MCP add_tags tool when mutation tools are explicitly enabled.</p>`;q('#persist').addEventListener('click',persistReview)}catch(e){q('#result').innerHTML=`<div class="empty"><h3>Analysis failed</h3><p>${esc(e.message)}</p></div>`}finally{btn.disabled=false;btn.innerHTML='Analyze blast radius <span>↗</span>'}});
async function persistReview(){if(!lastAnalysis)return;const btn=q('#persist'),status=q('#persistStatus');btn.disabled=true;btn.textContent='Writing metadata…';try{const r=await fetch('/api/review',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({urn:lastAnalysis.urn,riskLevel:lastAnalysis.risk.level,score:lastAnalysis.risk.value,changeType:lastAnalysis.changeType,detail:lastAnalysis.detail})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Write-back failed');status.innerHTML=d.persisted?`<strong class="ok">✓ ${esc(d.message)}</strong> <code>${esc(d.tagUrn)}</code>`:`<strong>${esc(d.message)}</strong> <code>${esc(d.tagUrn)}</code>`;btn.textContent=d.persisted?'Review persisted':'Preview write-back'}catch(e){status.textContent=e.message;btn.textContent='Retry write-back'}finally{btn.disabled=false}}
